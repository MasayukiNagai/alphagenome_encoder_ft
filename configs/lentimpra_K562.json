{
  "_comment": "Optimal hyperparameters for LentiMPRA K562 cell line. Fill in values after benchmarking.",
  "_source": "Generated from benchmark results",

  "cell_type": "K562",

  "data": {
    "batch_size": 32,
    "random_shift": true,
    "random_shift_likelihood": 0.5,
    "reverse_complement": true,
    "pad_n_bases": 0
  },

  "model": {
    "center_bp": 256,
    "pooling_type": "flatten",
    "nl_size": "1024",
    "do": 0.1,
    "activation": "relu"
  },

  "training": {
    "num_epochs": 100,
    "learning_rate": 0.001,
    "optimizer": "adam",
    "weight_decay": 1e-6,
    "gradient_accumulation_steps": 1,
    "gradient_clip": null,
    "lr_scheduler": null,
    "no_val_split": false,
    "early_stopping_patience": 5,
    "val_eval_frequency": 4,
    "test_eval_frequency": 4
  },

  "two_stage": {
    "enabled": true,
    "second_stage_lr": 1e-05,
    "second_stage_epochs": 50
  },

  "cached_embeddings": {
    "use_cached_embeddings": false,
    "cache_file": null
  },

  "checkpointing": {
    "checkpoint_dir": "./results/models/checkpoints/"
  },

  "wandb": {
    "enabled": true,
    "project": "alphagenome-mpra",
    "wandb_name": "mpra-K562"
  },

  "base_checkpoint_path": null
}
